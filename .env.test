# Test Database Configuration
# This file is used by the test suite to connect to the local Docker PostgreSQL instance

# Test Database (Docker PostgreSQL)
POSTGRES_URL="postgresql://hopescroll_test:test_password_local_only@localhost:5433/hopescroll_test"
POSTGRES_URL_NON_POOLING="postgresql://hopescroll_test:test_password_local_only@localhost:5433/hopescroll_test"

# Redis Cache (Mock/Optional for tests)
UPSTASH_REDIS_URL="redis://localhost:6379"
UPSTASH_REDIS_TOKEN="test-token"

# Vercel Blob Storage (Mock for tests)
BLOB_READ_WRITE_TOKEN="test-blob-token"

# External Content APIs (Mock for tests)
YOUTUBE_API_KEY="test-youtube-api-key"
TWITCH_CLIENT_ID="test-twitch-client-id"
TWITCH_CLIENT_SECRET="test-twitch-client-secret"

# Authentication (NextAuth)
NEXTAUTH_SECRET="test-secret-for-local-testing-only-not-for-production"
NEXTAUTH_URL="http://localhost:3000"

# Email (Resend - Mock for tests)
RESEND_API_KEY="re_test_api_key"

# Google OAuth (Mock for tests)
GOOGLE_CLIENT_ID="test-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="test-google-client-secret"

# Background Jobs Security
CRON_SECRET="test-cron-secret"

# Logging
LOG_LEVEL="error"  # Reduce noise in tests

# Feature Flags
ENABLE_USAGE_TRACKING="false"
ENABLE_PALETTE_CLEANSER="false"
